namespace D365FOSecurityComparison
{
    public enum LayerType
    {
        Role = 1,
        Duty = 2,
        Privilege = 3
    }

    public enum Action
    {
        Add = 0,
        Modify = 1,
        Remove = 2
    }
}
